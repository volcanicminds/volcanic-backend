export default function load() {
  describe('e2e', () => {
    // No end-to-end cases yet. Add `./<name>.ts` files exporting a default
    // function and import + invoke them here (static imports, sync describe).
  })
}
